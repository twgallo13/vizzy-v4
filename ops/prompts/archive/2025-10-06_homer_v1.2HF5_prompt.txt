Normalize Slack guards in .github/workflows/cicd.yml. Remove any secrets.* or env.* from job-level if—use only github.* there (staging: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'; production: startsWith(github.ref, 'refs/tags/')). Map required secrets to job env:. Gate deploy and Slack steps with step-level if using env.* (env.FIREBASE_TOKEN, env.STAGING_URL/env.PRODUCTION_URL, env.SLACK_WEBHOOK). Keep curl-based Slack notifications.